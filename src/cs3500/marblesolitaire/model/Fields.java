package cs3500.marblesolitaire.model;

/**
 * Enum that represents three different types of fields.
 */
public enum Fields {
  // represents marble status: X as empty, O as marble, and INVALID as not a valid position.
  X, O, INVALID
}
